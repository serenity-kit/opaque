{
  "name": "opaque",
  "version": "0.0.0",
  "author": "",
  "license": "MIT",
  "scripts": {
    "build": "pnpm run build:bundler && pnpm run build:node",
    "build:bundler": "wasm-pack build --out-dir dist/bundler",
    "build:node": "wasm-pack build --target nodejs --out-dir dist/node"
  },
  "devDependencies": {
    "prettier": "^2.8.7"
  }
}
