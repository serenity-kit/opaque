{
  "name": "opaque-monorepo",
  "version": "0.3.0",
  "license": "MIT",
  "devDependencies": {
    "@babel/core": "^7.21.4",
    "@babel/preset-env": "^7.21.4",
    "@babel/preset-typescript": "^7.21.4",
    "@rollup/plugin-terser": "^0.4.3",
    "@rollup/plugin-wasm": "^6.1.3",
    "babel-jest": "^29.5.0",
    "cross-env": "^7.0.3",
    "jest": "^29.5.0",
    "prettier": "^2.8.7",
    "rollup": "^3.23.0"
  },
  "scripts": {
    "build:wasm": "node ./bin/build.js",
    "build:bundle:ristretto": "cross-env BUILD_ENTRY=ristretto pnpm rollup -c",
    "build:bundle:p256": "cross-env BUILD_ENTRY=p256 pnpm rollup -c",
    "build:bundle": "pnpm build:bundle:ristretto && pnpm build:bundle:p256",
    "build:pkginfo": "node ./bin/pkginfo.js",
    "build": "pnpm build:wasm && pnpm build:bundle && pnpm build:pkginfo",
    "example:client-simple-webpack:dev": "cd examples/client-simple-webpack && pnpm dev",
    "example:client-simple-vite:dev": "cd examples/client-simple-vite && pnpm dev",
    "example:server:dev": "cd examples/server-simple && pnpm dev",
    "test:p256": "cross-env OPAQUE_BUILD=p256 jest",
    "test:ristretto": "jest",
    "test": "pnpm test:p256 && pnpm test:ristretto",
    "publish:ristretto": "cd build/ristretto && pnpm publish",
    "publish:p256": "cd build/p256 && pnpm publish",
    "publish": "pnpm publish:ristretto && pnpm publish:p256"
  }
}
